import React, { useState } from "react";

export default function ImageModal({ show }: { show: boolean }) {
  return (
    <div className="container mt-16 bg-slate-50 w-96 h-[500px]  mx-auto flex justify-center shadow-md rounded-md ">
      <div></div>
    </div>
  );
}
